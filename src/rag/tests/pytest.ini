[pytest]
# Pytest configuration for RAG tests
# GCS error messages are suppressed via conftest.py print filtering
addopts = -q --tb=line --maxfail=5
# Note: Add -n auto for parallel execution if pytest-xdist is installed
markers =
    unit: Unit tests
    integration: Integration tests
    system: System tests
    slow: Slow tests

